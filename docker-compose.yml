services:
  dashboard:
    build: .
    ports:
      - "8050:8050"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./display_names.json:/app/display_names.json:ro
    restart: unless-stopped
